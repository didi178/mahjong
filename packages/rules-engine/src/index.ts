/**
 * Rules Engine Package
 *
 * Pure game state reducer: (state, event) → (state', facts[])
 * Handles turns, Charleston, calls, Jokers, and winning.
 */

export const placeholder = 'rules-engine package scaffolded';
