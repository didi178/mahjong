# Source map — auto-generated by repo_map::regenerate_repo_map (#3739, #3758).
# Lean per-file index of desktop/src-tauri/src (Rust) + desktop/src
# (Svelte/TS) so agents resolve "where does X live / what's in module Y"
# without a grep fan-out, then read targeted slices instead of whole hub files.
# Schema: [[file]] { path, size_bytes, lines, purpose, symbols }.
# `path` is repo-root-relative; each symbol is "name:line:kind" where line
# is the 1-based definition line within path. Verified by CI gate.

